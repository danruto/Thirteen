#include "StratA.h"

// Strat A is to play next highest hand
std::vector<int> StratA::cardsToPlay(std::vector<Card> hand, std::vector<Card> lastPlayed)
{
	vector<int> positions;

	for (int i = 0; i < hand.size(); i++)
	{
		
	}

	return positions;
}